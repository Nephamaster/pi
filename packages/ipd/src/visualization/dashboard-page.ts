import type { DashboardSnapshot } from "./dashboard-model.ts";

export interface DashboardPageOptions {
	title?: string;
	runId: string;
	liveEndpoint?: string;
	snapshotUrl?: string;
	initialSnapshot?: DashboardSnapshot;
}

export function renderDashboardPage(options: DashboardPageOptions): string {
	const initial = options.initialSnapshot ? safeJson(options.initialSnapshot) : "null";
	const liveEndpoint = safeJson(options.liveEndpoint ?? null);
	const snapshotUrl = safeJson(options.snapshotUrl ?? null);
	const runId = safeJson(options.runId);
	const title = escapeHtml(options.title ?? "IPD Run Observatory");
	return `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>${title}</title>
<style>
:root{color-scheme:dark;--bg:#0a0d12;--panel:#111720;--panel2:#151d28;--line:#273345;--text:#e8edf5;--muted:#93a1b5;--accent:#6ea8fe;--ok:#55d187;--warn:#e5b454;--bad:#ff6b6b}
*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 15% -10%,#172238 0,transparent 32%),var(--bg);font:14px/1.5 Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:var(--text)}a{color:inherit}.shell{max-width:1500px;margin:0 auto;padding:24px}.top{display:flex;gap:20px;align-items:flex-start;justify-content:space-between;margin-bottom:18px}.eyebrow{font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:var(--accent);font-weight:700}.title{font-size:28px;font-weight:720;letter-spacing:-.02em;margin:3px 0}.sub{color:var(--muted);max-width:850px;word-break:break-all}.actions{display:flex;gap:8px;flex-wrap:wrap;justify-content:flex-end}.btn{appearance:none;border:1px solid var(--line);background:var(--panel2);color:var(--text);padding:9px 12px;border-radius:9px;text-decoration:none;cursor:pointer;font-weight:600}.btn:hover{border-color:#52647e}.live{display:inline-flex;align-items:center;gap:6px;color:var(--muted)}.dot{width:8px;height:8px;border-radius:50%;background:var(--muted)}.live.on .dot{background:var(--ok);box-shadow:0 0 0 5px rgba(85,209,135,.09)}.card{border:1px solid var(--line);background:linear-gradient(180deg,rgba(20,28,39,.96),rgba(14,20,28,.96));border-radius:14px;padding:16px;min-width:0}.grid{display:grid;grid-template-columns:1.2fr .8fr;gap:14px;margin-top:14px}.section-title{display:flex;justify-content:space-between;gap:10px;align-items:center;margin-bottom:11px}.section-title h2{font-size:14px;margin:0}.meta{color:var(--muted);font-size:12px}.task{font-size:16px;line-height:1.55;margin:4px 0 12px;white-space:pre-wrap}.list{margin:0;padding-left:18px}.list li{margin:5px 0}.kv{display:grid;grid-template-columns:max-content 1fr;gap:6px 12px}.kv .k{color:var(--muted)}.tag{font-size:11px;padding:3px 7px;border-radius:999px;border:1px solid var(--line);background:#0d131c;color:#b9c6d8}.status{font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.06em;padding:4px 8px;border-radius:999px;border:1px solid currentColor}.s-running,.s-active,.s-ready{color:var(--accent)}.s-succeeded{color:var(--ok)}.s-blocked,.s-failed{color:var(--bad)}.s-waiting_rework,.s-waiting_review{color:var(--warn)}.s-planned,.s-waiting{color:var(--muted)}.stages{display:grid;grid-template-columns:repeat(6,minmax(110px,1fr));gap:8px}.stage{border:1px solid var(--line);background:#0d131b;border-radius:10px;padding:10px 12px;color:var(--muted);min-height:57px}.stage b{display:block;font-size:12px}.stage small{font-size:10px;opacity:.8}.stage.done{border-color:rgba(85,209,135,.5);color:var(--ok)}.stage.current{border-color:rgba(110,168,254,.9);color:#dce9ff;background:#101b2a;box-shadow:0 0 0 1px rgba(110,168,254,.15) inset}.stage.error{border-color:rgba(255,107,107,.8);color:var(--bad)}.workflow-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.metrics{display:flex;gap:8px;flex-wrap:wrap}.metric{background:#0d131b;border:1px solid var(--line);border-radius:9px;padding:8px 10px;min-width:90px}.metric b{font-size:17px;display:block}.metric span{font-size:10px;color:var(--muted);text-transform:uppercase;letter-spacing:.07em}.graph-wrap{margin-top:12px;border:1px solid var(--line);border-radius:12px;background:#080c12;overflow:auto;min-height:360px}.graph-wrap svg{display:block}.legend{display:flex;gap:14px;flex-wrap:wrap;color:var(--muted);font-size:11px;margin-top:8px}.legend i{display:inline-block;width:16px;height:2px;vertical-align:middle;margin-right:5px;background:#52647e}.legend i.rework{height:0;border-top:2px dashed var(--bad);background:none}.node-detail{margin-top:12px;display:none}.node-detail.show{display:block}.node-detail .cols{display:grid;grid-template-columns:1fr 1fr;gap:12px}.empty{color:var(--muted);padding:22px;text-align:center}.events{max-height:390px;overflow:auto}.event{display:grid;grid-template-columns:78px 175px 1fr;gap:10px;padding:8px 0;border-top:1px solid rgba(39,51,69,.7);align-items:start}.event:first-child{border-top:0}.event .seq{color:var(--muted);font-variant-numeric:tabular-nums}.event .type{font-weight:650}.event .data{color:#aebbd0;word-break:break-word}.errorbox{border:1px solid rgba(255,107,107,.45);background:rgba(255,107,107,.08);border-radius:9px;padding:10px;color:#ffc2c2;margin-top:10px}.hidden{display:none!important}details{margin-top:9px}summary{cursor:pointer;color:#b9c6d8}.foot{color:var(--muted);font-size:11px;margin:16px 2px}@media(max-width:900px){.grid{grid-template-columns:1fr}.top{flex-direction:column}.actions{justify-content:flex-start}.stages{grid-template-columns:repeat(2,1fr)}.node-detail .cols{grid-template-columns:1fr}.event{grid-template-columns:60px 130px 1fr}}
</style>
</head>
<body>
<div class="shell">
  <div class="top"><div><div class="eyebrow">IPD Visualization</div><div class="title">IPD Run Observatory</div><div class="sub" id="run-subtitle">Run ${escapeHtml(options.runId)}</div></div><div class="actions"><span class="live" id="live-indicator"><span class="dot"></span><span id="live-label">Snapshot</span></span><button class="btn" id="refresh-btn">Refresh</button><a class="btn hidden" id="snapshot-btn" href="#">Download current snapshot</a></div></div>
  <div class="card"><div class="stages" id="stages"></div><div id="run-failure"></div></div>
  <div class="grid"><section class="card"><div class="section-title"><h2>User Task</h2><span class="meta" id="task-meta"></span></div><div class="task" id="task-text">Waiting for TaskInput…</div><div id="task-detail"></div></section><section class="card"><div class="section-title"><h2>ST · Process Selection</h2><span id="selection-status"></span></div><div id="selection-body" class="empty">Waiting for ST selection…</div></section></div>
  <section class="card" style="margin-top:14px"><div class="workflow-head"><div><div class="section-title" style="margin:0"><h2>Workflow Design & Execution</h2></div><div class="meta" id="workflow-meta">Waiting for Workflow Designer…</div></div><div class="metrics" id="workflow-metrics"></div></div><div class="graph-wrap" id="graph-wrap"><div class="empty">The workflow graph appears as soon as the Designer creates nodes.</div></div><div class="legend"><span><i></i>forward dependency</span><span><i class="rework"></i>review rework route</span><span>click a node for details</span></div><div class="node-detail card" id="node-detail"></div></section>
  <section class="card" style="margin-top:14px"><div class="section-title"><h2>Runtime Events</h2><span class="meta" id="event-meta"></span></div><div class="events" id="events"><div class="empty">No events yet.</div></div></section><div class="foot" id="generated-at"></div>
</div>
<script>
const RUN_ID=${runId};const LIVE_ENDPOINT=${liveEndpoint};const SNAPSHOT_URL=${snapshotUrl};let currentData=${initial};let selectedNodeId=null;
const PHASES=[["intake","Intake"],["selection","Process Selection"],["design","Workflow Design"],["compile","Compile"],["execute","Execute"],["closed","Closed"]];
const esc=value=>String(value??"").replace(/[&<>"']/g,ch=>ch.charCodeAt(0)===38?"&amp;":ch.charCodeAt(0)===60?"&lt;":ch.charCodeAt(0)===62?"&gt;":ch.charCodeAt(0)===34?"&quot;":"&#39;");const statusClass=value=>"status s-"+String(value||"planned").replace(/[^a-z_]/gi,"_").toLowerCase();
function displayPhase(data){if(data.run.phase==="intake"&&!data.selection.processSpec&&data.run.status==="running")return"selection";return data.run.phase}
function render(data){currentData=data;renderHeader(data);renderStages(data);renderTask(data);renderSelection(data);renderWorkflow(data);renderEvents(data);document.getElementById("generated-at").textContent="View generated "+new Date(data.generatedAt).toLocaleString()+" · state revision "+data.run.revision}
function renderHeader(data){document.getElementById("run-subtitle").innerHTML="Run <b>"+esc(data.run.id)+"</b> · phase <b>"+esc(displayPhase(data))+"</b> · status <b>"+esc(data.run.status)+"</b>";const live=Boolean(LIVE_ENDPOINT);document.getElementById("live-indicator").classList.toggle("on",live);document.getElementById("live-label").textContent=live?"Live":"Standalone snapshot";const snap=document.getElementById("snapshot-btn");if(SNAPSHOT_URL){snap.href=SNAPSHOT_URL;snap.classList.remove("hidden")}else snap.classList.add("hidden")}
function renderStages(data){const phase=displayPhase(data),idx=Math.max(0,PHASES.findIndex(([id])=>id===phase));document.getElementById("stages").innerHTML=PHASES.map(([id,label],i)=>{let cls="stage";if(i<idx||(phase==="closed"&&i<=idx))cls+=" done";if(i===idx&&phase!=="closed")cls+=" current";if(i===idx&&["blocked","failed"].includes(data.run.status))cls+=" error";const note=id==="selection"?"ST":id==="design"?"Designer":id==="execute"?"Workers + Reviews":"";return'<div class="'+cls+'"><b>'+esc(label)+'</b><small>'+esc(note)+'</small></div>'}).join("");document.getElementById("run-failure").innerHTML=data.run.failure?'<div class="errorbox"><b>'+esc(data.run.failure.code)+'</b><br>'+esc(data.run.failure.message)+'</div>':""}
function renderTask(data){const t=data.task;if(!t){document.getElementById("task-text").textContent="Waiting for TaskInput…";return}document.getElementById("task-text").textContent=t.text;document.getElementById("task-meta").textContent=t.requirements.length+" requirements · "+t.materials.length+" materials";const req=t.requirements.length?'<h3 style="font-size:12px">Requirements</h3><ul class="list">'+t.requirements.map(r=>'<li><span class="tag">'+esc(r.id)+'</span> '+esc(r.text)+'</li>').join("")+'</ul>':"";const facts=t.unresolvedFacts.length?'<h3 style="font-size:12px">Unresolved facts</h3><ul class="list">'+t.unresolvedFacts.map(f=>'<li><span class="tag">'+esc(f.id)+'</span> '+esc(f.description)+'</li>').join("")+'</ul>':"";document.getElementById("task-detail").innerHTML=req+facts}
function renderSelection(data){const s=data.selection;document.getElementById("selection-status").innerHTML='<span class="'+statusClass(s.status==="selected"?"succeeded":s.status)+'">'+esc(s.status)+'</span>';const body=document.getElementById("selection-body");if(s.status!=="selected"||!s.processSpec){body.className="empty";body.textContent=s.status==="blocked"?"Process selection is blocked; inspect the failure and events.":"ST is searching and evaluating ProcessSpec candidates.";return}body.className="";const p=s.processSpec;body.innerHTML='<div style="font-size:18px;font-weight:700">'+esc(p.name)+'</div><div class="meta">'+esc(p.id)+' @ '+esc(p.version)+'</div><p>'+esc(p.description)+'</p><div class="kv"><div class="k">Why selected</div><div>'+esc(s.rationale||"")+'</div><div class="k">Governance</div><div>'+p.requiredActivityCount+' activities · '+p.requiredDeliverableCount+' deliverables · '+p.requiredReviewCount+' reviews</div></div><details><summary>Applicability considered by ST</summary><div style="margin-top:8px"><b>Applicable when</b><ul class="list">'+p.applicableWhen.map(x=>'<li>'+esc(x)+'</li>').join("")+'</ul><b>Not applicable when</b><ul class="list">'+p.notApplicableWhen.map(x=>'<li>'+esc(x)+'</li>').join("")+'</ul></div></details>'}
function renderWorkflow(data){const w=data.workflow;const stateText=w.source==="compiled"?"Compiled execution baseline":w.source==="candidate"?"Compiler candidate":w.source==="draft"?"Designer draft":"Waiting for design";const bits=[stateText];if(w.name)bits.push(w.name);if(w.draftRevision!==undefined)bits.push("draft revision "+w.draftRevision);document.getElementById("workflow-meta").textContent=bits.join(" · ");const succeeded=w.nodes.filter(n=>n.status==="succeeded").length,active=w.nodes.filter(n=>n.status==="active").length;document.getElementById("workflow-metrics").innerHTML=metric(w.nodes.length,"nodes")+metric(w.criteriaCount,"criteria")+metric(w.coverageCount,"coverage")+metric(succeeded+"/"+w.nodes.length,"done")+(active?metric(active,"active"):"");drawGraph(w.nodes,w.edges);if(selectedNodeId&&w.nodes.some(n=>n.id===selectedNodeId))renderNodeDetail(w.nodes.find(n=>n.id===selectedNodeId));else document.getElementById("node-detail").classList.remove("show")}
function metric(value,label){return'<div class="metric"><b>'+esc(value)+'</b><span>'+esc(label)+'</span></div>'}
function drawGraph(nodes,edges){const wrap=document.getElementById("graph-wrap");if(!nodes.length){wrap.innerHTML='<div class="empty">The workflow graph appears as soon as the Designer creates nodes.</div>';return}const dependency=edges.filter(e=>e.kind==="dependency"),ids=new Set(nodes.map(n=>n.id)),indeg=new Map(nodes.map(n=>[n.id,0])),out=new Map(nodes.map(n=>[n.id,[]]));dependency.forEach(e=>{if(ids.has(e.from)&&ids.has(e.to)){indeg.set(e.to,(indeg.get(e.to)||0)+1);out.get(e.from).push(e.to)}});const q=nodes.filter(n=>indeg.get(n.id)===0).map(n=>n.id),layer=new Map(q.map(id=>[id,0]));let qi=0;while(qi<q.length){const id=q[qi++],base=layer.get(id)||0;for(const next of out.get(id)||[]){layer.set(next,Math.max(layer.get(next)||0,base+1));indeg.set(next,(indeg.get(next)||1)-1);if(indeg.get(next)===0)q.push(next)}}nodes.forEach(n=>{if(!layer.has(n.id))layer.set(n.id,0)});const groups=new Map();nodes.forEach(n=>{const l=layer.get(n.id)||0;if(!groups.has(l))groups.set(l,[]);groups.get(l).push(n)});const box={w:250,h:90},gapX=85,gapY=46,pad=34,maxLayer=Math.max(...layer.values()),maxRows=Math.max(...[...groups.values()].map(g=>g.length));const width=pad*2+(maxLayer+1)*box.w+maxLayer*gapX,height=Math.max(330,pad*2+maxRows*box.h+(maxRows-1)*gapY),pos=new Map();[...groups.entries()].sort((a,b)=>a[0]-b[0]).forEach(([l,group])=>{const total=group.length*box.h+(group.length-1)*gapY;let y=(height-total)/2;group.forEach(n=>{pos.set(n.id,{x:pad+l*(box.w+gapX),y});y+=box.h+gapY})});const ns="http://www.w3.org/2000/svg",svg=document.createElementNS(ns,"svg");svg.setAttribute("width",width);svg.setAttribute("height",height);svg.setAttribute("viewBox","0 0 "+width+" "+height);const defs=document.createElementNS(ns,"defs");defs.innerHTML='<marker id="arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse"><path d="M 0 0 L 10 5 L 0 10 z" fill="#66758b"/></marker><marker id="reworkArrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse"><path d="M 0 0 L 10 5 L 0 10 z" fill="#ff6b6b"/></marker>';svg.appendChild(defs);edges.forEach(e=>{const a=pos.get(e.from),b=pos.get(e.to);if(!a||!b)return;const p=document.createElementNS(ns,"path"),forward=e.kind==="dependency",x1=forward?a.x+box.w:a.x+box.w/2,y1=forward?a.y+box.h/2:a.y+box.h,x2=forward?b.x:b.x+box.w/2,y2=forward?b.y+box.h/2:b.y+box.h,bend=forward?(x1+x2)/2:Math.max(a.y,b.y)+box.h+30;p.setAttribute("d",forward?("M "+x1+" "+y1+" C "+bend+" "+y1+", "+bend+" "+y2+", "+x2+" "+y2):("M "+x1+" "+y1+" C "+x1+" "+bend+", "+x2+" "+bend+", "+x2+" "+y2));p.setAttribute("fill","none");p.setAttribute("stroke",forward?"#66758b":"#ff6b6b");p.setAttribute("stroke-width","1.6");if(!forward)p.setAttribute("stroke-dasharray","6 5");p.setAttribute("marker-end",forward?"url(#arrow)":"url(#reworkArrow)");svg.appendChild(p)});nodes.forEach(n=>{const p=pos.get(n.id),g=document.createElementNS(ns,"g");g.style.cursor="pointer";const r=document.createElementNS(ns,"rect");r.setAttribute("x",p.x);r.setAttribute("y",p.y);r.setAttribute("width",box.w);r.setAttribute("height",box.h);r.setAttribute("rx","11");r.setAttribute("fill",nodeFill(n.status,n.kind));r.setAttribute("stroke",nodeStroke(n.status,n.kind));r.setAttribute("stroke-width",n.status==="active"?"2.4":"1.2");g.appendChild(r);svgText(g,p.x+14,p.y+22,n.kind.toUpperCase()+" · "+n.status,10,"#93a1b5",700);svgText(g,p.x+14,p.y+45,short(n.name,30),14,"#e8edf5",700);svgText(g,p.x+14,p.y+66,short(n.agent,34),11,"#b7c4d6",500);svgText(g,p.x+14,p.y+82,n.roundCount?"rounds "+n.roundCount:"not started",9,"#7f90a8",500);g.addEventListener("click",()=>{selectedNodeId=n.id;renderNodeDetail(n)});svg.appendChild(g)});wrap.replaceChildren(svg)}
function nodeFill(status,kind){if(status==="active")return"#132844";if(status==="succeeded")return"#10251c";if(status==="blocked")return"#2a1519";if(status==="waiting_rework"||status==="waiting_review")return"#292314";return kind==="review"?"#171326":"#111821"}function nodeStroke(status,kind){if(status==="active")return"#6ea8fe";if(status==="succeeded")return"#55d187";if(status==="blocked")return"#ff6b6b";if(status==="waiting_rework"||status==="waiting_review")return"#e5b454";return kind==="review"?"#8069bf":"#334257"}function svgText(g,x,y,value,size,color,weight){const el=document.createElementNS("http://www.w3.org/2000/svg","text");el.setAttribute("x",x);el.setAttribute("y",y);el.setAttribute("font-size",size);el.setAttribute("font-family","Inter,system-ui,sans-serif");el.setAttribute("fill",color);el.setAttribute("font-weight",weight);el.textContent=value;g.appendChild(el)}function short(value,n){const s=String(value||"");return s.length>n?s.slice(0,n-1)+"…":s}
function renderNodeDetail(n){const box=document.getElementById("node-detail");if(!n){box.classList.remove("show");return}box.classList.add("show");box.innerHTML='<div class="section-title"><h2>'+esc(n.name)+'</h2><span class="'+statusClass(n.status)+'">'+esc(n.status)+'</span></div><div class="kv"><div class="k">Node</div><div>'+esc(n.id)+'</div><div class="k">Kind</div><div>'+esc(n.kind)+'</div><div class="k">Employee</div><div>'+esc(n.agent)+'</div><div class="k">Objective</div><div>'+esc(n.objective)+'</div></div><div class="cols" style="margin-top:12px"><div><b>Responsibilities</b><ul class="list">'+n.responsibilities.map(x=>'<li>'+esc(x)+'</li>').join("")+'</ul></div><div><b>Inputs / Outputs</b><div class="meta" style="margin-top:6px">Inputs</div><ul class="list">'+n.inputs.map(x=>'<li>'+esc(x)+'</li>').join("")+'</ul><div class="meta">Outputs / targets</div><ul class="list">'+n.outputs.map(x=>'<li>'+esc(x)+'</li>').join("")+'</ul></div></div>'}
function renderEvents(data){document.getElementById("event-meta").textContent=data.events.length+" recent events";const root=document.getElementById("events");if(!data.events.length){root.innerHTML='<div class="empty">No events yet.</div>';return}root.innerHTML=[...data.events].reverse().map(e=>'<div class="event"><div class="seq">#'+e.sequence+'<br>'+new Date(e.timestamp).toLocaleTimeString()+'</div><div class="type">'+esc(e.type)+(e.nodeId?'<br><span class="tag">'+esc(e.nodeId)+'</span>':'')+'</div><div class="data">'+esc(JSON.stringify(e.data))+'</div></div>').join("")}
async function refresh(){if(!LIVE_ENDPOINT){if(currentData)render(currentData);return}try{const response=await fetch(LIVE_ENDPOINT,{cache:"no-store"});if(!response.ok)throw new Error(await response.text());render(await response.json())}catch(err){document.getElementById("generated-at").textContent="Live refresh failed: "+String(err)}}document.getElementById("refresh-btn").addEventListener("click",refresh);if(currentData)render(currentData);if(LIVE_ENDPOINT){refresh();setInterval(refresh,1200)}
</script>
</body>
</html>`;
}

function safeJson(value: unknown): string {
	return JSON.stringify(value).replaceAll("<", "\\u003c").replaceAll(">", "\\u003e").replaceAll("&", "\\u0026");
}

function escapeHtml(value: string): string {
	return value.replace(/[&<>"']/g, (character) => ({
		"&": "&amp;",
		"<": "&lt;",
		">": "&gt;",
		'"': "&quot;",
		"'": "&#39;",
	})[character] ?? character);
}
